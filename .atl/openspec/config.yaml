schema: spec-driven

context: |
  Tech stack: Python 3.14.2 (scripts, HUBs), Node.js (OpenCode plugins)
  Architecture: Personal OS system with skill-based workflows
  Testing: None detected (no test runner at project root)
  Style: Conventional commits, no linter/formatter detected

strict_tdd: false

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules/packages
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (infrastructure, implementation, testing)
    - Use hierarchical numbering (1.1, 1.2, etc.)
    - Keep tasks small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions
    - Load relevant coding skills for the project stack
  verify:
    - Run tests if test infrastructure exists
    - Compare implementation against every spec scenario
  archive:
    - Warn before merging destructive deltas (large removals)

testing:
  strict_tdd: false
  detected: "2026-04-03"
  test_runner:
    available: false
    framework: null
  test_layers:
    - layer: unit
      available: false
      tool: null
    - layer: integration
      available: false
      tool: null
    - layer: e2e
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality_tools:
    - tool: linter
      available: false
      command: null
    - tool: type_checker
      available: false
      command: null
    - tool: formatter
      available: false
      command: null